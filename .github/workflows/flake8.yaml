name: flake8 Lint

on: [pull_request]

jobs:
  flake8-lint:
    runs-on: alpine_latest
    name: Flake8
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@master
        with:
          python-version: 3.8
